RETIRED HISTORICAL RELEASE - DO NOT EXECUTE

This marker replaces the August 2026 release instructions. It must never be used to restore retired provider limits or eligibility gates.

Current sources of truth:
- AGENTS.md
- db/policy/canonical_operational_policy.sql
- docs/fila-continua.md
- docs/contabilidade-melo-disparo.md
- ops/README.md

Current mandatory invariants:
- production provider is Brevo Transactional Email;
- effective hard cap and target are at most 300 submissions per rolling 24h;
- external/control submissions count through `mei_email.envios_externos_cota`;
- canonical first-contact policy comes only from AGENTS.md;
- MG is ordering priority, not an eligibility filter;
- queue buffer is controlled by `QUEUE_MIN_PENDING`/`QUEUE_TARGET_PENDING` and a single global replenishment lock;
- accepted provider submission is `submitted` until reconciled delivery evidence exists;
- `mei-mg-email-ndr-guard.service` remains disabled;
- never remove technical suppressions, anti-replay history, opt-outs, or the sender-block sentinel automatically.
